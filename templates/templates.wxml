<template name="multi-cells">
<view class="zan-panel-title">{{date}}</view>
  <view class="zan-panel">
    <div class="weui-cell">
      <div class="weui-cell__bd">
        <p>{{time}}</p>
      </div>
      <div class="weui-cell__ft2">{{place}}</div>
      <div class="weui-cell__ft2">{{money}}</div>
    </div>
  </view>
</template>